box: wercker/nodejs
build:
  steps:
    - npm-install
    - script:
        name: build
        code: npm run build
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: luke.vivier.ca
        basedir: build
